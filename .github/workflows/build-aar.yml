name: Build FFmpegKit Universal AAR

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout ffmpeg-kit
        uses: actions/checkout@v4.2.2

      - name: Setup Java 17
        uses: actions/setup-java@v4.7.0
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Download NDK r27c
        run: |
          wget -q https://dl.google.com/android/repository/android-ndk-r27c-linux.zip -O ndk.zip
          unzip -q ndk.zip -d $HOME
          echo "ANDROID_NDK_ROOT=$HOME/android-ndk-r27c" >> $GITHUB_ENV

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            autoconf automake libtool pkg-config \
            curl git doxygen nasm \
            gcc g++ gperf texinfo yasm \
            bison autogen wget autopoint \
            meson ninja-build ragel groff \
            gtk-doc-tools libtasn1-6-dev \
            build-essential

      - name: Pre-clone cpu-features and fix cmake compatibility
        run: |
          mkdir -p src
          git clone --depth=1 --branch v0.8.0 \
            https://github.com/arthenica/cpu_features.git \
            src/cpu-features

          CMAKE_FILE="src/cpu-features/CMakeLists.txt"
          sed -i \
            's/cmake_minimum_required(VERSION [^)]*)/cmake_minimum_required(VERSION 3.5)/g' \
            "$CMAKE_FILE"
          sed -i 's/if\s*(BUILD_TESTING)/if(FALSE)/g' "$CMAKE_FILE"

          echo "=== Verifying cmake patches ==="
          grep -n "cmake_minimum_required\|BUILD_TESTING\|if(FALSE)" "$CMAKE_FILE"

      - name: Wrap NDK clang to inject page-size linker flags
        run: |
          NDK_BIN="$ANDROID_NDK_ROOT/toolchains/llvm/prebuilt/linux-x86_64/bin"

          # NDK r27c drives LLD in-process inside clang — wrapping ld.lld is useless.
          # We must intercept clang itself and inject flags only on link steps.

          CLANG_REAL="$(readlink -f "$NDK_BIN/clang")"
          echo "Wrapping: $CLANG_REAL"
          cp "$CLANG_REAL" "${CLANG_REAL}.real"

          cat > "$CLANG_REAL" << WRAPPER
#!/bin/bash
REAL="${CLANG_REAL}.real"
for arg in "\$@"; do
  case "\$arg" in
    -c|-E|-S|-M|-MM|-fsyntax-only|--version)
      exec -a "\$0" "\$REAL" "\$@"
      ;;
  esac
done
exec -a "\$0" "\$REAL" "\$@" -Wl,-z,max-page-size=16384 -Wl,-z,common-page-size=16384
WRAPPER

          chmod +x "$CLANG_REAL"
          echo "=== Wrapper installed at: $CLANG_REAL ==="
          cat "$CLANG_REAL"
          "$NDK_BIN/clang" --version

      - name: Patch all clang 18 / NDK r27c compatibility issues
        run: |
          python3 << 'PYEOF'
          import os, glob, datetime

          BUILD_DATE = datetime.datetime.now().strftime('%Y%m%d')
          print(f"BUILD_DATE = {BUILD_DATE}")

          CFLAGS = [
              '-Wno-pointer-sign',
              '-Wno-single-bit-bitfield-constant-conversion',
              f'-DFFMPEG_KIT_BUILD_DATE={BUILD_DATE}',
          ]

          LDFLAGS_MK = [
              '-Wl,-z,max-page-size=16384',
              '-Wl,-z,common-page-size=16384',
          ]

          MARKER = 'clang18-pagesz-fix'

          def patch_application_mk(path):
              content = open(path).read()
              if MARKER in content:
                  print(f"  Already patched: {path}")
                  return
              additions = f'\n# {MARKER}\n'
              for flag in CFLAGS:
                  additions += f'APP_CFLAGS += {flag}\n'
              for flag in LDFLAGS_MK:
                  additions += f'APP_LDFLAGS += {flag}\n'
              open(path, 'a').write(additions)
              print(f"  Patched {path}")
              print(open(path).read())

          def patch_android_mk(path):
              content = open(path).read()
              if MARKER in content:
                  print(f"  Already patched: {path}")
                  return
              cflag_line = 'LOCAL_CFLAGS += ' + ' '.join(CFLAGS)
              ldflag_line = 'LOCAL_LDFLAGS += ' + ' '.join(LDFLAGS_MK)
              lines = content.splitlines(keepends=True)
              out = []
              for line in lines:
                  out.append(line)
                  if line.strip().startswith('LOCAL_MODULE') and ':=' in line:
                      out.append(f'# {MARKER}\n')
                      out.append(cflag_line + '\n')
                      out.append(ldflag_line + '\n')
              open(path, 'w').writelines(out)
              print(f"  Patched {path}")

          def prepend_pragma(path, pragma_flag):
              pragma = f'#pragma clang diagnostic ignored "{pragma_flag}"\n'
              content = open(path).read()
              if pragma not in content:
                  open(path, 'w').write(pragma + content)
                  print(f"  Prepended pragma {pragma_flag} to {path}")

          for f in glob.glob('**/Application.mk', recursive=True):
              patch_application_mk(f)

          for f in glob.glob('**/Android.mk', recursive=True):
              patch_android_mk(f)

          for f in glob.glob('**/ffmpegkit.c', recursive=True):
              prepend_pragma(f, '-Wpointer-sign')

          for f in glob.glob('**/fftools_ffmpeg_mux_init.c', recursive=True):
              prepend_pragma(f, '-Wsingle-bit-bitfield-constant-conversion')

          print('\nAll patches applied.')
          PYEOF

      - name: Build Universal AAR
        run: |
          ./android.sh \
            --enable-gpl \
            --enable-x264 \
            --disable-arm-v7a \
            --disable-arm-v7a-neon \
            --disable-x86 \
            --disable-x86-64
          cp prebuilt/bundle-android-aar/ffmpeg-kit/ffmpeg-kit.aar ffmpeg-kit-universal.aar

      - name: Verify .so alignment and RELRO
        run: |
          python3 << 'PYEOF'
          import struct, zipfile

          def inspect(data):
              if data[:4] != b'\x7fELF' or data[4] != 2:
                  return 0, None, True  # skip non-64-bit ELF gracefully
              e_phoff     = struct.unpack_from('<Q', data, 32)[0]
              e_phentsize = struct.unpack_from('<H', data, 54)[0]
              e_phnum     = struct.unpack_from('<H', data, 56)[0]
              max_load_align = 0
              relro_vaddr = None
              load_segments = []
              for i in range(e_phnum):
                  off     = e_phoff + i * e_phentsize
                  p_type  = struct.unpack_from('<I', data, off)[0]
                  p_vaddr = struct.unpack_from('<Q', data, off + 16)[0]
                  p_filesz= struct.unpack_from('<Q', data, off + 32)[0]
                  p_align = struct.unpack_from('<Q', data, off + 48)[0]
                  if p_type == 1:
                      max_load_align = max(max_load_align, p_align)
                      load_segments.append((p_vaddr, p_filesz, p_align))
                  if p_type == 0x6474e552:
                      relro_vaddr = p_vaddr
              relro_aligned = (relro_vaddr is None) or (relro_vaddr % 4096 == 0)
              return max_load_align, relro_vaddr, relro_aligned

          print("=== .so segment verification ===")
          print(f"{'Library':<35} {'PT_LOAD align':>14} {'RELRO vaddr':>12} {'RELRO 4KB?':>10}")
          all_ok = True
          with zipfile.ZipFile('ffmpeg-kit-universal.aar') as z:
              for name in sorted(z.namelist()):
                  if not name.endswith('.so'):
                      continue
                  data = z.read(name)
                  load_align, relro_vaddr, relro_ok = inspect(data)
                  lib = name.split('/')[-1]
                  align_str = f"{load_align//1024}KB" if load_align >= 1024 else f"{load_align}B"
                  relro_str = f"0x{relro_vaddr:x}" if relro_vaddr else "none"
                  ok_str = "OK" if relro_ok else "MISALIGNED"
                  if not relro_ok:
                      all_ok = False
                  print(f"  {lib:<33} {align_str:>14} {relro_str:>12} {ok_str:>10}")

          print()
          if all_ok:
              print("All RELRO segments are page-aligned. Safe to ship.")
          else:
              print("RELRO misalignment detected. The Poco F1 crash WILL recur.")
              raise SystemExit(1)
          PYEOF

      - name: Print build.log on failure
        if: failure()
        run: |
          find . -name "build.log" -exec cat {} \;

      - name: Upload Universal AAR
        uses: actions/upload-artifact@v4.6.2
        with:
          name: ffmpeg-kit-universal
          path: ffmpeg-kit-universal.aar
          retention-days: 30
